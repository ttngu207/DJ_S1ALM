%{
# 
cell_type   :  varchar(100)
---
description :  varchar(4000)
%}


classdef CellType < dj.Lookup
     properties
        contents = {
            'Putative pyramidal' ''
            'FS' 'fast spiking'
            'not classified' 'cells with intermediate spike-width that fell between spike-width thresholds FS and Pyramidal thresholds'
            }
    end
end